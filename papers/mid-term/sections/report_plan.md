# 五、下一步研究计划与论文工作计划

## 5.1 技术路线

### 5.1.1 提升修复能力

当前 MACR 在 QuixBugs 上的修复率为 55%，下一步将从以下方向提升：

1. **跨文件缺陷修复**：扩展 KG 的构建范围，支持多文件、跨模块依赖分析，以处理 Defects4J 等更真实的数据集 [@just_defects4j_2014]。QuixBugs 中的缺陷多为单文件、局部逻辑错误，而 Defects4J 中的缺陷往往涉及多个文件和复杂的业务逻辑。为此，我们需要：
   - 改进 KG 构建算法，支持项目级依赖分析，包括包导入、类继承和跨文件函数调用。
   - 开发上下文选择策略，在生成补丁时自动检索相关文件和函数。
   - 设计针对 Java 语言的 AST 解析和 KG 表示方案。

2. **强化反馈机制**：引入更细粒度的测试失败定位与补丁差异分析，提升 Diagnosis Agent 的建议质量。具体措施包括：
   - 使用切片技术定位失败测试中受补丁影响的关键语句。
   - 对比正确执行与失败执行的轨迹差异，生成更准确的诊断报告。
   - 将诊断信息编码为结构化模板，便于生成智能体直接利用。

3. **智能采样策略**：结合 RepairAgent 与 SWE-agent 的工具调用思想，在生成前主动检索相关文件与文档，减少上下文不足导致的失败 [@bouzenia_repairagent_2024; @yang_swe-agent_2024]。我们计划实现以下工具：
   - 代码搜索工具：根据关键词或失败信息搜索相关代码片段。
   - 文档检索工具：从 README、API 文档和代码注释中获取上下文。
   - 版本历史工具：分析 git 历史以发现类似修复模式。

4. **模型能力提升**：探索 DeepSeek-Coder-V2 等更强大的代码模型在 MACR 框架下的表现 [@deepseek-ai_deepseek-coder-v2_2024]。我们将对比不同模型在相同任务上的修复率、成本和迭代次数，并分析模型规模与修复能力的关系。

### 5.1.2 多智能体协同机制优化

针对 Cemri 等人揭示的多智能体失败模式 [@cemri_why_nodate]，MACR 将重点优化以下方面：

- **任务规范强化**：在协调器中为每个子智能体设定更明确的输入输出规范与终止条件。我们将为每个智能体编写详细的角色描述和示例提示，减少任务理解偏差。

- **智能体对齐机制**：引入交叉检查（cross-verification）与一致性约束，减少智能体间信息偏差。例如，Patch Generation Agent 生成的补丁可由一个独立的 Review Agent 进行初步审查，验证其是否符合代码风格和语义约束。

- **动态角色调整**：借鉴 AgentDropout 的自适应智能体选择策略 [@qian_scaling_2025]，根据问题复杂度动态增减参与修复的智能体数量。对于简单缺陷，可减少智能体数量以降低成本；对于复杂缺陷，可增加 Review Agent 和 Test Generation Agent 以提高成功率。

- **通信效率优化**：当前协调器采用集中式通信，所有信息通过协调器中转。我们将探索更高效的通信模式，如直接智能体间通信和共享记忆机制，以减少重复信息传递。

### 5.1.3 知识图谱扩展

代码 KG 的覆盖范围与质量直接影响 MACR 的效果。下一步工作包括：

- 引入程序依赖图（PDG）与控制流图（CFG）信息，丰富 KG 的语义表达能力。PDG 能够表示数据依赖和控制依赖，有助于理解缺陷的传播路径；CFG 能够表示执行路径，有助于分析测试覆盖和边界条件。

- 探索 KG 与检索增强生成（RAG）的结合，将外部 API 文档与项目特定知识纳入上下文。例如，当缺陷涉及某个库函数时，可从官方文档中检索该函数的用法和常见错误。

- 研究 KG 的增量更新策略，在补丁生成过程中动态反映代码变化。当前 KG 在每个修复轮次重新构建，效率较低。我们将开发增量更新算法，仅更新受补丁影响的节点和关系。

- 引入图神经网络（GNN）对 KG 进行嵌入学习，使智能体能够利用图结构进行更精确的推理和候选选择。

## 5.2 论文工作计划

| 时间节点 | 任务 | 预期产出 |
|---------|------|---------|
| 2025 年 7 月 | 完成论文初稿 | 完整论文手稿 |
| 2025 年 8 月 | 内部审稿与修改 | 修改后论文 |
| 2025 年 9 月 | 补充 Defects4J 实验 | 扩展实验结果 |
| 2025 年 10 月 | 论文精修与格式调整 | 投稿版本 |
| 2025 年 12 月 | 投稿 ICSE 2026 | 投稿确认 |

: 下一步工作计划 {#tab:plan}

表\ref{tab:plan} 列出了下一阶段的主要工作计划。

除主会投稿外，我们还计划将 MACR 的部分技术模块投稿至相关 workshop，例如：

- **LLM4Code Workshop @ ICSE 2026**：聚焦 LLM 在代码任务中的应用，适合展示 MACR 的 LLM-based 修复机制。
- **AIware Workshop @ FSE 2026**：关注 AI 驱动的软件工程系统，适合讨论 MACR 的多智能体架构设计。

## 5.3 风险评估与应对措施

| 风险 | 影响 | 应对措施 |
|------|------|---------|
| Defects4J 实验成本过高 | 时间延误 | 采用分层采样，优先验证高可疑度缺陷；使用本地模型降低 API 成本 |
| 多智能体通信不稳定 | 修复率波动 | 固化协调协议，增加状态检查点；引入重试和超时机制 |
| 论文审稿周期长 | 错过目标会议 | 同时准备 FSE 2026 和 EMNLP 2026 等备选会议 |
| 新模块引入回归问题 | 修复率下降 | 建立回归测试集，在添加新功能前验证基线性能 |

: 项目风险与应对措施 {#tab:risk}

表\ref{tab:risk} 列出了项目潜在风险与应对措施。

## 5.4 总结

本阶段工作围绕 MACR 框架的设计、实现与初步评估展开，已在 QuixBugs 数据集上取得 55% 的修复率。下一步将重点提升跨文件修复能力、优化多智能体协同机制，并在更真实的数据集上验证方法有效性，力争在 ICSE 2026 或 FSE 2026 上发表高水平论文。

从长远来看，MACR 的研究不仅限于程序修复，还可扩展至代码审查、测试生成、缺陷预测等更广泛的软件工程任务。通过不断完善多智能体协同机制和知识图谱技术，我们期望为智能化软件开发提供新的方法论和工具支持。
